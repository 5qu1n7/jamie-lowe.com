---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary' | 'accent' | 'outline';
  target?: string;
  rel?: string;
}

const { href, variant = 'primary', target, rel } = Astro.props;
---

<a href={href} class={`btn btn-${variant}`} {target} {rel}>
  <slot />
</a>
